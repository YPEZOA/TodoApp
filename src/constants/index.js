import { COLORS } from '../theme/colors/colors'

const labels = {
    TODO: { text: 'To do', color: COLORS.todo_label },
    INPROGRESS: { text: 'In progress', color: COLORS.inprogress_label },
    DONE: { text: 'Done', color: COLORS.done_label }
}

export { labels }
