import { Text } from '@chakra-ui/react'
import { Layout } from '../Layout'

export const Main = () => {
    return (
        <Layout>
            <Text>Weta</Text>
        </Layout>
    )
}

Main.displayName = 'Main'
